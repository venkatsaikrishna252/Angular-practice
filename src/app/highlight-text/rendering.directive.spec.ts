import { RenderingDirective } from './rendering.directive';

describe('RenderingDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderingDirective();
    expect(directive).toBeTruthy();
  });
});
