import { StuctureDirectiveDirective } from './stucture-directive.directive';

describe('StuctureDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new StuctureDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
